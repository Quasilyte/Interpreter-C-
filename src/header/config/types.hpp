#ifndef HEADER_CONFIG_TYPES_
#define HEADER_CONFIG_TYPES_

#include <stdint.h>

// #TODO: check if platform supports given exact-width types from `stdint'

typedef unsigned char byte;

typedef int32_t i32;

#endif
